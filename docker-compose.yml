services:
  # university_auth:
  #   build:
  #     context: ./university_auth
  #     dockerfile: Dockerfile
  #   env_file:
  #     - ./university_auth/.env
  #   environment:
  #     - DATABASE_URL=postgresql+asyncpg://qwey189t:7opsd95sd@shared_db:5432/university_shared_db
  #     - APP_CONFIG__RABBIT__URL=amqp://ag62efg:lidf43r@rabbitmq:5672/
  #   networks:
  #     - university_network
  #   depends_on:
  #     shared_db:
  #       condition: service_healthy
  #     rabbitmq:
  #       condition: service_healthy

  university_organization:
    build:
      context: ./university_organization
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./university_organization/.env
    environment:
      - DATABASE_URL=postgresql+asyncpg://qwey189t:7opsd95sd@shared_db:5432/university_shared_db
      - APP_CONFIG__RABBIT__URL=amqp://ag62efg:lidf43r@rabbitmq:5672/
    networks:
      - university_network
    depends_on:
      shared_db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

  university_student_test:
    build:
      context: ./university_student_test
      dockerfile: Dockerfile

    ports:
      - "8010:8010"
    env_file:
      - ./university_student_test/.env
    environment:
      - DATABASE_URL=postgresql+asyncpg://qwey189t:7opsd95sd@shared_db:5432/university_shared_db
      - APP_CONFIG__RABBIT__URL=amqp://ag62efg:lidf43r@rabbitmq:5672/
    networks:
      - university_network
    depends_on:
      - university_organization
      - shared_db
      - rabbitmq

  shared_db:
    image: postgres:15
    environment:
      - POSTGRES_USER=qwey189t
      - POSTGRES_PASSWORD=7opsd95sd
      - POSTGRES_DB=university_shared_db
    volumes:
      - shared_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U qwey189t -d university_shared_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - university_network

  # student_test_db:
  #   image: postgres:15
  #   environment:
  #     - POSTGRES_USER=i6arf3t
  #     - POSTGRES_PASSWORD=odks953
  #     - POSTGRES_DB=university_student_db
  #   volumes:
  #     - student_test_db_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U i6arf3t -d university_student_db"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 10s
  #   networks:
  #     - university_network

  # rabbitmq:
  #   image: rabbitmq:3.12-management
  #   environment:
  #     - RABBITMQ_DEFAULT_USER=ag62efg
  #     - RABBITMQ_DEFAULT_PASS=lidf43r
  #   volumes:
  #     - rabbitmq_data:/var/lib/rabbitmq
  #   healthcheck:
  #     test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 20s
  #   networks:
  #     - university_network

  gateway:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - university_auth
      - university_organization
      - university_student_test
    networks:
      - university_network

volumes:
  shared_db_data:
  # student_test_db_data:
  # rabbitmq_data:

networks:
  university_network:
    driver: bridge
